### Todo
- [x] Putting params into the RF
- [x] Scale down MLP and RFF
- [x] Combine features via concatenation of outputs of each mlp layer
- [x] Hold Kaczmarz first for Direct Pseudoinverse
- [ ] Make Tikhonov Regularization a trainable parameter
- [ ] Try different activation functions eg tanh, sin, sinh

### Daily Report
* **Wednesday** - Change to direct pseudoinverse and change feature extractor network architecture

## Introduction
Last week, we injected parameters at every layer of the MLP feature generator. This lead to each sample in the batch of 8 to not have their features be separated very cleanly, meaning the feature matrix probably looked similar for all 8 samples. Hence, the results were not that good. Now, we treat the params like the coordinates too, and pass them into the RFF at the very start. Hopefully, this makes the feature matrix more conditioned on the parameters. So, given a batch of 8 samples, we will have 8 different $A$ matrices and 8 different $b$ target vectors (the source $s$). Previously with Kaczmarz, we would solve them individually and then average out the new weights of each sample to be the new weights of the epoch. But now, we shall try to concat all the A matrix together and solve it directly with pseudoinverse, meaning we vertically stack the matrices using only the 1,000 sampled spatial points per sample. However this might OOM, so we can just do the gram assembly first then feed it into jax to direct solve.

Additionally we shall try these modifications:
1. Scale down num_features of RFF and MLP (number of nodes of RFF > MLP nodes)
2. Combine features via concatenation of outputs of each mlp layer
3. Try different activation functions eg. tanh, sin, sinh
4. Make Tikhonov Regularization a trainable parameter

#### Random Fourier Features
$v$ is the concatenated input vector:
$$v = \begin{bmatrix} x \\ y \\ p \end{bmatrix} \in \mathbb{R}^{2 + |p|}$$

$B$ is the random projection matrix sampled from a normal distribution with variance $\sigma^2$, where $M$ is the number of frequencies:
$$B \sim \mathcal{N}(0, \sigma^2)^{(2 + |p|) \times M}$$
 
The RFF projection, $h_0$, is computed as:
$$h_0 = \begin{bmatrix} \sin(2\pi B^T v) \\ \cos(2\pi B^T v) \end{bmatrix} \in \mathbb{R}^{2M}$$

So, if we have `M = 256`, we would have $2 \times 256 = 512$ features.

In addition, we concat the outputs of each layer of the MLP to the resulting features. Moreover, we also add a skip connection from the input of the MLP to the output of the MLP. This is the result:

RFF: $M = 256$ frequencies $\rightarrow$ $512$ RFF nodes
MLP: $4$ hidden layers of $128$ nodes $\rightarrow$ $512$ MLP nodes
Total Features for Solver: $1024$

This makes it so that we have equal features from RFF and MLP.

#### New role of $w$
**With Sketch-And-Project:**
$w$ is a trainable and persists across epochs. It is updated slowly. We find a single $w$ that works well enough for every single sample in the dataset. For evaluation, we use the final "averaged" $w$ learned during training to evaluate new validation samples.

**With Direct Solve:**
$w$ is no longer something we train, but the analytical solution calculated on the fly. It does not update. It is just the answer of the for $w = (A^T A + \lambda I)^{-1} A^T b$. In this setup we are training the MLP to generate the perfect features such that the direct solver can calculate the  (the 1,024 features). For evaluation, we solve for a brand new $w$ specifically for the validation samples to test if the MLP's output features are actually good.
To plot the results, we pull the specific sample and its parameters, solve for $w$ with a small chunk of coordinates, then push the entire $128 \times 128$ grid through your MLP to get the 1,024 features for the whole sample. Then we just do the final dot product with the solved $w$ to get the final $u_{\text{pred}}$ values.

### Problems

## Results
#### Wednesday
With direct pseudoinverse, coordinates + params -> RFF
```
epochs = 5
batch_size = 1024
M = 256
chunk_size = 256
hidden_layers = [128, 128, 128, 128] 
total_features = (2 * M) + sum(hidden_layers) # 512 + 512 = 1024
``` 
Epoch 001 | Train Loss (PDE+BC): 1.5005e-03 | Validation MSE: 1.4644e-08
Epoch 002 | Train Loss (PDE+BC): 1.0846e-03 | Validation MSE: 1.2996e-08
Epoch 003 | Train Loss (PDE+BC): 9.0306e-04 | Validation MSE: 1.1469e-08
Epoch 004 | Train Loss (PDE+BC): 7.8764e-04 | Validation MSE: 1.1302e-08
Epoch 005 | Train Loss (PDE+BC): 7.1406e-04 | Validation MSE: 1.1018e-08

<img src="../assets/wk5/wednesday-results.png" width="1000">